apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  finalizers:
    - kubernetes.io/pvc-protection
  name: books
  namespace: calibre
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: rook-newfs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  finalizers:
    - kubernetes.io/pvc-protection
  name: config
  namespace: calibre
spec:
  accessModes:
    # SQLite DB...
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-newfs